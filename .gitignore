# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

references/
# Local secrets
.env

# Worker scratch (video downloads + intermediates). WORK_DIR in .env is
# resolved against the worker's cwd, so this lives under worker/.
worker/output/
worker/.work/
worker/.venv/
